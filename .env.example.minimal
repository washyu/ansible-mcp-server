# MCP Server Configuration
# This is a minimal configuration file
# Run 'setup-wizard' in Claude to configure interactively

# Proxmox Configuration (required for VM operations)
PROXMOX_HOST=
PROXMOX_USER=root@pam
PROXMOX_PASSWORD=
PROXMOX_NODE=proxmox
PROXMOX_VERIFY_SSL=false

# Network Configuration (required for VM networking)
DEFAULT_GATEWAY=
DEFAULT_NAMESERVER=8.8.8.8
DEFAULT_NETWORK_CIDR=24

# Storage Configuration
DEFAULT_STORAGE=local-lvm
DEFAULT_BRIDGE=vmbr0

# SSE Server Configuration (for remote access)
SSE_PORT=3001
API_ACCESS_TOKEN=change-me-to-secure-token

# Optional: Service IP assignments
# Configure these through setup-services tool
# NEXTCLOUD_IP=
# MAILSERVER_IP=
# KEYCLOAK_IP=
# NGINX_IP=